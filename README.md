# SaaSBase Frontend

这是 `SaaSBase` 前端工作区的根目录。

## 安装

```bash
pnpm install
```

## 常用命令

```bash
pnpm lint
pnpm typecheck
pnpm test
pnpm build
```

## 说明

- 这是一个 `pnpm workspace` 根项目
- 当前阶段先搭建工作区和基础配置
- 后续应用与共享包会按 `apps/*` 和 `packages/*` 扩展
